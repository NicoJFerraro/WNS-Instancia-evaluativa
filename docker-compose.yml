# docker-compose.yml

services:
  api:
    build: .
    ports:
      - "5001:5001"
    volumes:
      - ./database:/app/database
      - ./inputs:/app/inputs:ro  # Read-only
    environment:
      - FLASK_ENV=production
    command: python -m src.api.app